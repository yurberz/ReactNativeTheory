export enum KeyStorage {
  UserProfile = '@userProfile',
}

export enum URLType {
  api = 'https://',
}

export enum Domain {
  unsplash = 'api.unsplash.com/',
}

export enum RequestType {
  fetchPhotos,
  searchPhotos,
  likePhoto,
  unlikePhoto,
}
